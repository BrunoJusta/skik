import styles from "../../styles/Tag.module.scss";
import { useState, useEffect, useRef } from "react";

const Tag = (props) => {
  useEffect(() => {}, []);

  return <div className={styles.tag}>skik</div>;
};

export default Tag;
